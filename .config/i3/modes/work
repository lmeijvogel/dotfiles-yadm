mode "$work_mode" {
      bindsym f $exec konsole -e bash $HOME/bin/lib/feature-flags-to-clipboard.sh ; mode "default"

      # back to normal: enter or escape
      bindsym Return mode "default"
      bindsym Escape mode "default"
}
